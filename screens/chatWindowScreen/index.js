import React from 'react'

import ChatWindow from '../../components/chatWindow'

export default ChatWindowScreen = ({ route, navigation }) => {
  return <ChatWindow route={route} navigation={navigation} />
}
